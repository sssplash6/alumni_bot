# job-queue extra: the gate's background jobs (form poll, announcement
# re-post, DB backups) need JobQueue — without it app.job_queue is None and
# they silently never run.
python-telegram-bot[job-queue]==21.10
aiosqlite==0.20.0
python-dotenv==1.0.1
httpx>=0.27,<0.29
pytest==8.3.5
pytest-asyncio==0.24.0
